class Effect {
  constructor(type, originCell, targetCell, min, max) {
    this.type = type;
    this.originCell = originCell;
    this.targetCell = targetCell;
    this.min = min;
    this.max = max;
  }
}